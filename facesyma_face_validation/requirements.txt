# Face Validation Microservice Requirements

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Image processing — headless variant avoids libGL display deps in Docker
opencv-python-headless==4.8.1.78
numpy==1.26.4

# Face detection (bundles its own TFLite runtime — no tensorflow-lite needed)
mediapipe==0.10.9

# Serialisation
pydantic==2.5.0
